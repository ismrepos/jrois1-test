os: linux
language: node_js
node_js:
- "10"
addons:
  chrome: stable
  firefox: "71.0"
sudo: required
cache: npm

before_install:
- "npm i -g npm@latest"
- "npm -v"
- "cd Rois/test/"

install:
- "npm install"

before_script:
- "date"

script: "npm test"
