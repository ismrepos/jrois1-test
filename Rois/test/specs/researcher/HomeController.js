const assert = require('assert')

describe.skip('研究者ホーム画面', () => {
    browser.url(browser.config.moduleURL+browser.config.loginURL)
    it('研究者アカウントでホーム画面に遷移できること', () => {
        
    })
    //'ログイン機関名確認'
    //'お知らせテーブル表示確認'
    //'公募一覧テーブル表示確認'
    //'モーダル動作確認'
    //'申請一覧テーブル表示確認'
    //'モーダル動作確認'
    //'メニュー動作確認?'
})
